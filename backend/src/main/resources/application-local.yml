spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coachdb
    username: coach
    password: coach
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.back.coach: DEBUG
    org.hibernate.SQL: DEBUG
